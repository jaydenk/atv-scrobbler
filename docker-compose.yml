services:
  atv-scrobbler:
    image: ghcr.io/jaydenk/atv-scrobbler:latest
    container_name: atv-scrobbler
    restart: unless-stopped
    # Host networking required for Bonjour/mDNS discovery and MRP ephemeral ports
    network_mode: host
    mem_limit: 128m
    memswap_limit: 128m
    environment:
      - PYTHONUNBUFFERED=1
      # pyatv reads credentials from $HOME/.pyatv.conf
      - HOME=/app/data
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
      - /etc/localtime:/etc/localtime:ro
    healthcheck:
      test: ["CMD-SHELL", "find /app/heartbeat -mmin -1 | grep -q heartbeat"]
      interval: 30s
      timeout: 5s
      retries: 3
